# Python dependencies for the Assignment 2 pipeline scripts.
# Airflow itself is provided by the base image (apache/airflow:2.6.1-python3.10),
# so it is intentionally NOT listed here.
pyspark==3.5.5
numpy<2
pandas>=2.1,<2.3
pyarrow>=14.0.0
scikit-learn>=1.3
xgboost>=2.0
matplotlib>=3.7
